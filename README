mod_disconnect_script:
    Disconnect client via call external program/script

    Configuration:
    Add to dynamic modules section:
    {mod_disconnect_script, [{disconnect_script, "/full/path/to/script"}]}

mod_netflow_trace:
    Simple module for logging raw netflow data into files with rotation

    Configuration:
    Add to dynamic modules section:
    {mod_netflow_trace, [{trace_dir, "/full/path/to/trace/directory"}]}
